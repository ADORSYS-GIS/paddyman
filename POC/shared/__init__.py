"""Shared infrastructure for the data extraction pipeline.

Sub-packages:
- ``shared.config``  — central, environment-driven configuration (``settings``, ``require_env``)
- ``shared.models``  — domain contracts exchanged between pipeline stages
"""
